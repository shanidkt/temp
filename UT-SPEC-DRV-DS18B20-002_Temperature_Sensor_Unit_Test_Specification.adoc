= Unit Test Specification - DS18B20 Temperature Sensor Driver
:doctype: article
:toc: left
:toclevels: 3
:sectnums:
:sectnumlevels: 4
:icons: font
:source-highlighter: rouge
:imagesdir: images
:revnumber: 0.2
:revdate: 2026-07-30

:author: CIOET Firmware Team
:email: shanid@htic.iitm.ac.in
:docref: UT-SPEC-DRV-DS18B20-001

// ─────────────────────────────────────────────────────────────────────────────
// Document Control Table
// ─────────────────────────────────────────────────────────────────────────────

[cols="1,1,2,2,2", options="header"]
|===
|Version |Date       |Author  |Reviewer  |Change Summary

|0.1     |2026-07-30 |Shanid KT |         |Initial draft — `drv_ds18b20` (Init/Read) only
|0.2     |2026-07-30 |Shanid KT |         |Added coverage for the application-layer files touched under ORPT-118 (`ds18b20_task.c/h`, `mw_gpio.c` open-drain fix, FreeRTOS task registration, build config) — see <<files-covered,Files Covered by This Specification>>

|===

---

== Purpose

This document defines the unit-level test specification for the DS18B20 temperature-sensing path of the OrgPort S32K312 firmware, covering both the portable driver and every file changed on branch `ORPT-118-Temperature-sensor-DS18B20-implementation` to make it run on target:

* *drv_ds18b20* — DS18B20 1-Wire digital temperature sensor driver (`common_libs/drivers/sensors/ds18b20`)
* *ds18b20_task* — FreeRTOS application task that polls the driver and publishes the latest reading (`orgport-app-s32k312/app/ds18b20`)
* *mw_gpio* — the open-drain direction-switching fix required for the 1-Wire bus to physically work (`orgport-app-s32k312/middleware/gpio/mw_gpio.c`)
* FreeRTOS task registration and heap sizing changes needed to run the new task (`app/os/os_app_config.c`, `generate/include/FreeRTOSConfig.h`)

The purpose is to verify that this path behaves correctly — from the driver's public API (`DRV_DS18B20_Init`, `DRV_DS18B20_Read`) up through the FreeRTOS task that keeps a live reading available to the rest of the firmware — under nominal, boundary, and fault-injection conditions prior to integration testing on the S32K312 node.

[[files-covered]]
== Files Covered by This Specification

The table below lists every file touched to bring up the DS18B20 temperature sensor, traced against the actual commits/working-tree state, so this spec can be checked off file-by-file. "Status" reflects the state at the time this document was written (2026-07-30) — commit hashes and working-tree state should be re-confirmed before formal review, since work is still in progress.

[cols="3,2,1,3", options="header"]
|===
|File |Status |Touched By |Covered By

|`common-libs/drivers/sensors/ds18b20/drv_ds18b20.c` / `.h`
|Committed (`common-libs`, commit `0f7cc79`, "ORPT-63")
|Benin (senior) — pre-existing, not modified for ORPT-118
|§Init — NULL/Parameter Guards, §Init — Device Presence and Scratchpad CRC, §Read — NULL Guards, §Read — Bus Fault/CRC/Conversion, §Hardware Verification

|`common-libs/tests/test_drv_ds18b20.c`
|Committed (`common-libs`, commit `0f7cc79`)
|Benin (senior) — pre-existing host Unity suite
|All `UT-DS18B20-INIT-*` / `UT-DS18B20-READ-*` cases (one-to-one with the 13 `RUN_TEST()` entries)

|`orgport-app-s32k312/app/ds18b20/ds18b20_task.c` (new)
|Committed, branch `ORPT-118-...`, commit `4d65d8f`
|*Shanid KT*
|§Application Task — ds18b20_task

|`orgport-app-s32k312/app/ds18b20/ds18b20_task.h` (new)
|Committed, branch `ORPT-118-...`, commit `4d65d8f`
|*Shanid KT*
|§Application Task — ds18b20_task

|`orgport-app-s32k312/middleware/gpio/mw_gpio.c` (open-drain output-buffer fix)
|Committed, branch `ORPT-118-...`, commit `4d65d8f`
|*Shanid KT*
|§GPIO Open-Drain Direction Switching

|`orgport-app-s32k312/app/os/os_app_config.c` (task table entry for `APP_DS18B20_Task`)
|Committed, branch `ORPT-118-...`, commit `0410e7c`
|*Shanid KT*
|§Build and Scheduler Integration

|`orgport-app-s32k312/generate/include/FreeRTOSConfig.h` (`configTOTAL_HEAP_SIZE` 8192→16384)
|Committed, branch `ORPT-118-...`, commit `0410e7c`
|*Shanid KT* (shared change — also supports the stepper task added in the same commit)
|§Build and Scheduler Integration

|`orgport-app-s32k312/.cproject`, `.settings/language.settings.xml` (registers `app/ds18b20` as a build source folder)
|Committed, branch `ORPT-118-...`, commit `0410e7c`
|*Shanid KT*
|§Build and Scheduler Integration

|`orgport-app-s32k312/app/init/drivers_init.c` / `.h` (`s_ds18b20_cfg` population, `DRV_DS18B20_Init()` call, `APP_DS18B20_Cfg` accessor)
|Pre-existing, identical to baseline as of this review — *not modified* by Shanid
|Benin (senior), under ORPT-39 scaffolding
|§Init — Device Presence and Scratchpad CRC (config values only — see Open Items OI-005)

|`orgport-app-s32k312/app/gpio/GPIO_app_config.h`, `app/gpio/gpio_app_config.c` (`APP_GPIO_DS18B20_DQ_PORT/PIN`, GPIO table entry)
|Pre-existing, identical to baseline as of this review — *not modified* by Shanid
|Benin (senior), under ORPT-39 scaffolding
|§GPIO Open-Drain Direction Switching (pin assignment referenced only — see Open Items OI-005)

|===

*Not in scope of this specification* — bundled into the same ORPT-118 commit (`0410e7c`) but unrelated to the thermometer: `app/stepper/stepper_task.c/.h` (stepper motor task) and the `APP_WDG_NotifyAlive()` calls added to `app/timing/task_timing_test.c` (watchdog supervisor wiring). These are stepper/watchdog work swept into the same commit and are covered by their own test specifications, not this one.

== Scope

This specification covers white-box and black-box unit tests for `drv_ds18b20`, exercised on the host build via the `common-libs/tests` Unity harness; the `ds18b20_task` application task and its `mw_gpio` dependency, exercised on-target; and the on-target hardware verification tests required to close the acceptance criterion in OP-SDP-001 (task T-J2-07). It does not cover:

* `lib_onewire` bus-protocol correctness (covered by its own unit test suite, `test_lib_onewire.c`)
* Integration behaviour between `drv_ds18b20`/`ds18b20_task` and the workflow engine, FEM event reporting, `eeprom_mgr`, or CANopen TPDO packaging of `temp_1`/`temp_2` (covered by the Integration Test Specification)
* ROM search / multi-drop 1-Wire operation — the driver supports SKIP ROM (single device) only in this revision; ROM search is reserved for a future release
* The stepper motor task and watchdog-supervisor `NotifyAlive` wiring bundled into the same commit as the DS18B20 build-config changes (see <<files-covered,Files Covered by This Specification>>) — unrelated to the thermometer, covered by their own specs
* System-level or clinical performance testing

== Applicable Documents and Standards

[cols="1,3", options="header"]
|===
|Reference |Title

|IEC 62304:2006+AMD1:2015
|Medical device software — Software life cycle processes

|IEC 60601-1:2005+AMD1+AMD2
|Medical electrical equipment — General requirements for basic safety

|ISO 14971:2019
|Application of risk management to medical devices

|CIOET-ORGPORT-SWRS-2026-001
|OrgPort Software Requirements Specification (Master)

|OP-SDP-001
|OrgPort Software Development Plan (task T-J2-07, T-SR-26)

|CIOET-ORGPORT-CGD-2026-001
|C Coding Guidelines

|===

== Definitions and Abbreviations

[cols="1,3", options="header"]
|===
|Term |Definition

|CUT
|Code Under Test — the specific function or module being exercised

|DUT
|Device Under Test — the target S32K312 hardware board with a DS18B20 sensor attached

|CRC
|Cyclic Redundancy Check — CRC-8/MAXIM, validated over scratchpad bytes 0–7 against byte 8

|DRV_OK / true
|Driver status: operation succeeded (boolean `true` return)

|DRV_FAIL / false
|Driver status: operation failed — NULL parameter, no device presence, or CRC mismatch (boolean `false` return)

|HIL
|Hardware-in-the-Loop — Arduino-based 1-Wire slave emulator (`sim_ds18b20`) used to inject controlled temperature values and fault conditions on a real 1-Wire bus

|SKIP ROM
|1-Wire ROM command (0xCC) that addresses the single device on the bus without ROM search

|Scratchpad
|9-byte DS18B20 internal register block: 2 bytes temperature, TH, TL, config, 3 reserved, 1 CRC

|SWDP
|OrgPort Software Development Plan (OP-SDP-001)

|TC
|Test Case

|===

== Test Environment

=== Hardware

[cols="1,3", options="header"]
|===
|Item |Description

|DUT
|NXP S32K312 evaluation board (S32K312MINI-EVB), 1-Wire bus wired to a GPIO + DWT-timed Arduino header pin per OP-SDP-001 §5.2/§5.3

|DS18B20 sensor (reference unit)
|Genuine DS18B20 TO-92, 4.7 kΩ pull-up to 3.3 V, used for TC-DS18B20-HW-001/002

|HIL emulator
|Arduino board running `sim_ds18b20` (1-Wire slave emulator on pin D4) — supports `SIM_DS18B20_SetTemperature()` and `SIM_DS18B20_SetFault(no_presence)` for controlled fault injection, used for TC-DS18B20-HW-003/004

|Calibrated reference thermometer
|±0.1°C accuracy or better, same thermal mass/location as the DS18B20 under test

|Oscilloscope / Logic Analyser
|≥ 1 MHz sample rate, for 1-Wire timing and conversion-time verification

|Debug Probe
|NXP S32 Debug Probe or compatible J-Link (SWD interface)

|===

=== Software and Toolchain

[cols="1,3", options="header"]
|===
|Item |Description

|Host compiler
|GCC (`gcc`), `-std=c11 -Wall -Wextra -Werror`, per `common-libs/tests/Makefile`

|Unit Test Framework
|Unity (host-PC build) — `common-libs/tests/test_drv_ds18b20.c`, registered in `test_runner.c` as `test_drv_ds18b20_suite()`

|Stubs
|`stub_gpio` (array-backed GPIO state with scripted read sequences, `common-libs/tests/test_stub/stub_gpio.c/h`), `stub_time` (`stub_delay_us`, `stub_delay_ms`)

|Target IDE
|NXP S32 Design Studio 3.x or later (for on-target hardware tests)

|HIL firmware
|`cioet-dev-tools/hil/arduino/orgport_hil/sim_ds18b20.cpp/h`, Arduino IDE

|===

=== General Preconditions (apply to all test cases unless overridden)

. For host-build test cases: `stub_gpio_reset()` and `stub_time_reset()` have been called immediately before the test case (performed automatically by the `ds_setup()` helper in `test_drv_ds18b20.c`).
. For on-target test cases: the DUT is freshly power-cycled or reset before each test suite execution, `Clock_Ip_Init()` has been called, and the 1-Wire GPIO pin direction/pull configuration matches the peripheral allocation in OP-SDP-001 §5.2.
. `DRV_DS18B20_Config_t.resolution` is `DRV_DS18B20_RES_12BIT` (0.0625°C, 750 ms conversion) unless a test case explicitly varies it.
. The driver under test does not call `LIB_FEM_Report()` — fault reporting is the caller's responsibility and is out of scope for this specification.
. Test results are recorded in the Test Execution Report (document TER-DRV-DS18B20-001).

=== Pass / Fail Criteria

A test case passes when *all* expected results stated for each step are met with no deviation. Any single deviation constitutes a failure and must be recorded with an incident report reference.

[[automated-test-execution-procedure]]
=== Automated Test Execution Procedure

The 13 automated test cases in this specification (see the *Execution* column of the Test Traceability Matrix) are all exercised by a single host-build Unity run. This procedure is performed once and the resulting console output is the evidence attached for every automated test case below.

. From `common-libs/tests/`, run `make` (equivalently `make build run`). This compiles `test_runner.c`, all `test_*.c` suites (including `test_drv_ds18b20.c`), `drv_ds18b20.c`, `lib_onewire.c`, `lib_crc.c`, and the stub sources, then executes `./test_runner`.
. Unity prints one line per test case in the form `test_drv_ds18b20.c:<line>:test_<name>:PASS` (or `FAIL` with the assertion detail).
. Confirm the summary line at the end of the run reads `13 Tests 0 Failures 0 Ignored` for the `drv_ds18b20` suite (the run also reports totals for the other suites registered in `test_runner.c`).
. Capture **one** console log of the full run.
. For each automated test case below, locate its test function name in the captured console log and confirm the line ends `PASS`.

*Combined Test Evidence Log*

image::REPLACE_WITH_CONSOLE_LOG_FILENAME.png[Console output of `./test_runner` showing all drv_ds18b20 test cases PASS]

---

== Module: drv_ds18b20 — DS18B20 Temperature Sensor Driver

=== Overview

`drv_ds18b20` provides temperature acquisition from a single DS18B20 1-Wire digital sensor. It layers on top of `lib_onewire` (bus reset/presence, byte and block read/write) and `lib_crc` (CRC-8/MAXIM validation), and exposes two functions:

* `DRV_DS18B20_Init(cfg)` — verifies device presence, writes the resolution to the scratchpad configuration register, and reads back the scratchpad to confirm the write via CRC.
* `DRV_DS18B20_Read(cfg, out)` — issues Convert T (0x44), blocks for the resolution-dependent conversion time using the caller-supplied `delay_ms`, reads the 9-byte scratchpad, validates CRC-8 over bytes 0–7 against byte 8, and converts the raw 16-bit two's-complement value to °C (12-bit resolution: 1 LSB = 0.0625°C).

All platform dependencies (`gpio_set`, `gpio_read`, `gpio_set_dir`, `delay_us`, `delay_ms`) are received as function pointers in `DRV_DS18B20_Config_t` at call time — the driver contains no RTD or STM32 HAL calls, per the layering rules in OP-SDP-001 §4.3.

=== Init — NULL and Parameter Guards

==== UT-DS18B20-INIT-001 — NULL config pointer returns false

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-INIT-001

|Requirement
|SWRS-M1-SENS-01 (temperature channel 1) — _pending confirmation, see Open Items OI-001_

|Objective
|Verify that `DRV_DS18B20_Init(NULL)` is rejected before any 1-Wire bus access.

|Preconditions
|General preconditions apply.

|*Test Steps:* | Refer to <<automated-test-execution-procedure,Automated Test Execution Procedure>>. (Corresponds to `test_ds18b20_init_null_cfg_returns_false`.)

|*Expected Results:*| `DRV_DS18B20_Init(NULL)` returns `false`. `stub_gpio_set_count`, `stub_gpio_read_count` remain unchanged (no bus access attempted). Confirmed PASS in the console log captured per the Automated Test Execution Procedure.
|*Actual Results:*|
|===

---

==== UT-DS18B20-INIT-002 — NULL delay_ms returns false

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-INIT-002

|Requirement
|SWRS-M1-SENS-01 — _pending confirmation, see Open Items OI-001_

|Objective
|Verify that a config with `delay_ms == NULL` is rejected, since the conversion wait cannot be performed.

|Preconditions
|General preconditions apply. `cfg` built via `make_cfg()` then `cfg.delay_ms = NULL`.

|*Test Steps:* | Refer to <<automated-test-execution-procedure,Automated Test Execution Procedure>>. (Corresponds to `test_ds18b20_init_null_delay_ms_returns_false`.)

|*Expected Results:*| `DRV_DS18B20_Init(&cfg)` returns `false`. Confirmed PASS in the console log.
|*Actual Results:*|
|===

---

==== UT-DS18B20-INIT-003 — NULL ow.gpio_set returns false

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-INIT-003

|Requirement
|SWRS-M1-SENS-01 — _pending confirmation, see Open Items OI-001_

|Objective
|Verify that a config with `ow.gpio_set == NULL` is rejected.

|Preconditions
|General preconditions apply. `cfg` built via `make_cfg()` then `cfg.ow.gpio_set = NULL`.

|*Test Steps:* | Refer to <<automated-test-execution-procedure,Automated Test Execution Procedure>>. (Corresponds to `test_ds18b20_init_null_ow_gpio_set_returns_false`.)

|*Expected Results:*| `DRV_DS18B20_Init(&cfg)` returns `false`. Confirmed PASS in the console log.
|*Actual Results:*|
|===

---

==== UT-DS18B20-INIT-004 — NULL ow.gpio_read returns false

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-INIT-004

|Requirement
|SWRS-M1-SENS-01 — _pending confirmation, see Open Items OI-001_

|Objective
|Verify that a config with `ow.gpio_read == NULL` is rejected.

|Preconditions
|General preconditions apply. `cfg` built via `make_cfg()` then `cfg.ow.gpio_read = NULL`.

|*Test Steps:* | Refer to <<automated-test-execution-procedure,Automated Test Execution Procedure>>. (Corresponds to `test_ds18b20_init_null_ow_gpio_read_returns_false`.)

|*Expected Results:*| `DRV_DS18B20_Init(&cfg)` returns `false`. Confirmed PASS in the console log.
|*Actual Results:*|
|===

---

==== UT-DS18B20-INIT-005 — NULL ow.gpio_set_dir returns false

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-INIT-005

|Requirement
|SWRS-M1-SENS-01 — _pending confirmation, see Open Items OI-001_

|Objective
|Verify that a config with `ow.gpio_set_dir == NULL` is rejected.

|Preconditions
|General preconditions apply. `cfg` built via `make_cfg()` then `cfg.ow.gpio_set_dir = NULL`.

|*Test Steps:* | Refer to <<automated-test-execution-procedure,Automated Test Execution Procedure>>. (Corresponds to `test_ds18b20_init_null_ow_gpio_set_dir_returns_false`.)

|*Expected Results:*| `DRV_DS18B20_Init(&cfg)` returns `false`. Confirmed PASS in the console log.
|*Actual Results:*|
|===

---

==== UT-DS18B20-INIT-006 — NULL ow.delay_us returns false

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-INIT-006

|Requirement
|SWRS-M1-SENS-01 — _pending confirmation, see Open Items OI-001_

|Objective
|Verify that a config with `ow.delay_us == NULL` is rejected.

|Preconditions
|General preconditions apply. `cfg` built via `make_cfg()` then `cfg.ow.delay_us = NULL`.

|*Test Steps:* | Refer to <<automated-test-execution-procedure,Automated Test Execution Procedure>>. (Corresponds to `test_ds18b20_init_null_ow_delay_us_returns_false`.)

|*Expected Results:*| `DRV_DS18B20_Init(&cfg)` returns `false`. Confirmed PASS in the console log.
|*Actual Results:*|
|===

---

==== UT-DS18B20-INIT-007 — Out-of-range resolution returns false

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-INIT-007

|Requirement
|SWRS-M1-SENS-01 — _pending confirmation, see Open Items OI-001_

|Objective
|Verify that a `resolution` value outside the 4 defined enum members (0–3) is rejected before any bus access.

|Preconditions
|General preconditions apply. `cfg.resolution = (DRV_DS18B20_Res_e)4U`.

|*Test Steps:* | Refer to <<automated-test-execution-procedure,Automated Test Execution Procedure>>. (Corresponds to `test_ds18b20_init_invalid_resolution_returns_false`.)

|*Expected Results:*| `DRV_DS18B20_Init(&cfg)` returns `false`. Confirmed PASS in the console log.
|*Actual Results:*|
|===

---

=== Init — Device Presence and Scratchpad CRC

==== UT-DS18B20-INIT-008 — No device present on reset returns false

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-INIT-008

|Requirement
|SWRS-M1-SENS-01 — _pending confirmation, see Open Items OI-001_

|Objective
|Verify that `DRV_DS18B20_Init` fails cleanly when no device responds to the reset pulse (bus stays HIGH — open-drain pull-up with no slave present).

|Preconditions
|General preconditions apply. `stub_gpio_force_read = true`, `stub_gpio_read_val = true` (bus reads HIGH after the reset low pulse, simulating no presence pulse).

|*Test Steps:* | Refer to <<automated-test-execution-procedure,Automated Test Execution Procedure>>. (Corresponds to `test_ds18b20_init_no_device_returns_false`.)

|*Expected Results:*| `LIB_OneWire_Reset()` returns `false` (no presence pulse detected); `DRV_DS18B20_Init(&cfg)` returns `false` without attempting the scratchpad write. Confirmed PASS in the console log.
|*Actual Results:*|
|===

---

==== UT-DS18B20-INIT-009 — Device present, valid scratchpad CRC returns true

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-INIT-009

|Requirement
|SWRS-M1-SENS-01 — _pending confirmation, see Open Items OI-001_

|Objective
|Verify that `DRV_DS18B20_Init` succeeds end-to-end: presence detected, resolution written, scratchpad read back, and CRC-8 validated.

|Preconditions
|General preconditions apply. A 74-entry all-LOW GPIO read script is loaded via `stub_gpio_load_read_script()`, so both `LIB_OneWire_Reset()` calls report presence and the 9-byte scratchpad readback is all `0x00` (CRC-8 of eight `0x00` bytes is `0x00`, matching `scratch[8] = 0x00`).

|*Test Steps:* | Refer to <<automated-test-execution-procedure,Automated Test Execution Procedure>>. (Corresponds to `test_ds18b20_init_device_present_valid_crc_returns_true`.)

|*Expected Results:*| `DRV_DS18B20_Init(&cfg)` returns `true`. Confirmed PASS in the console log.
|*Actual Results:*|
|===

---

=== Read — NULL Guards

==== UT-DS18B20-READ-001 — NULL config pointer returns false

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-READ-001

|Requirement
|SWRS-M1-SENS-01 — _pending confirmation, see Open Items OI-001_

|Objective
|Verify that `DRV_DS18B20_Read(NULL, &out)` is rejected before any 1-Wire bus access.

|Preconditions
|General preconditions apply.

|*Test Steps:* | Refer to <<automated-test-execution-procedure,Automated Test Execution Procedure>>. (Corresponds to `test_ds18b20_read_null_cfg_returns_false`.)

|*Expected Results:*| `DRV_DS18B20_Read(NULL, &out)` returns `false`. Confirmed PASS in the console log.
|*Actual Results:*|
|===

---

==== UT-DS18B20-READ-002 — NULL output pointer returns false

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-READ-002

|Requirement
|SWRS-M1-SENS-01 — _pending confirmation, see Open Items OI-001_

|Objective
|Verify that `DRV_DS18B20_Read(&cfg, NULL)` is rejected.

|Preconditions
|General preconditions apply. `cfg` built via `make_cfg()`.

|*Test Steps:* | Refer to <<automated-test-execution-procedure,Automated Test Execution Procedure>>. (Corresponds to `test_ds18b20_read_null_out_returns_false`.)

|*Expected Results:*| `DRV_DS18B20_Read(&cfg, NULL)` returns `false`. Confirmed PASS in the console log.
|*Actual Results:*|
|===

---

=== Read — Bus Fault, CRC, and Conversion

==== UT-DS18B20-READ-003 — No device present on reset returns false

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-READ-003

|Requirement
|SWRS-M1-SENS-01 — _pending confirmation, see Open Items OI-001_

|Objective
|Verify that `DRV_DS18B20_Read` fails cleanly and reports `out->valid == false` when no device responds to the reset before the Convert T command is issued.

|Preconditions
|General preconditions apply. `stub_gpio_force_read = true`, `stub_gpio_read_val = true` (bus HIGH = no device).

|*Test Steps:* | Refer to <<automated-test-execution-procedure,Automated Test Execution Procedure>>. (Corresponds to `test_ds18b20_read_no_device_on_reset_returns_false`.)

|*Expected Results:*| `DRV_DS18B20_Read(&cfg, &out)` returns `false`. Confirmed PASS in the console log.
|*Actual Results:*|
|===

---

==== UT-DS18B20-READ-004 — All-zero scratchpad decodes to 0.0°C with valid CRC

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-READ-004

|Requirement
|SWRS-M1-SENS-01 — _pending confirmation, see Open Items OI-001_

|Objective
|Verify the full nominal read path: both bus resets succeed, the 9-byte scratchpad reads back all `0x00`, CRC-8 validates, the raw 16-bit value (`0x0000`) is converted using the 0.0625°C/LSB scale factor, and `out->valid` is set `true`.

|Preconditions
|General preconditions apply. A 74-entry all-LOW GPIO read script is loaded via `stub_gpio_load_read_script()`.

|*Test Steps:* | Refer to <<automated-test-execution-procedure,Automated Test Execution Procedure>>. (Corresponds to `test_ds18b20_read_all_zeros_returns_zero_celsius`.)

|*Expected Results:*| `DRV_DS18B20_Read(&cfg, &out)` returns `true`; `out.valid == true`; `out.temperature_c` is within ±0.001°C of `0.0f` (`TEST_ASSERT_FLOAT_WITHIN(0.001f, 0.0f, out.temperature_c)`). Confirmed PASS in the console log.
|*Actual Results:*|
|===

---

=== Application Task — ds18b20_task

`app/ds18b20/ds18b20_task.c/h` (new files, Shanid KT, commit `4d65d8f`) wraps the driver in a FreeRTOS task: `APP_DS18B20_Task()` loops forever calling `DRV_DS18B20_Read(APP_DS18B20_Cfg, &s_latest_reading)`, and `APP_DS18B20_GetLatest(out)` copies the last stored reading out to any caller (e.g. the workflow engine, TPDO packaging).

==== UT-DS18B20-TASK-001 — GetLatest with NULL output pointer is a no-op

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-TASK-001

|Requirement
|SWRS-M1-SENS-01 — _pending confirmation, see Open Items OI-001_

|Objective
|Verify that `APP_DS18B20_GetLatest(NULL)` does not dereference the NULL pointer and does not corrupt `s_latest_reading`.

|Preconditions
|`ds18b20_task.c` compiled as a host-testable unit (module-local `s_latest_reading` made visible to the test via a test-only accessor, or tested on-target with a debugger watch). Scheduler not required to be running for this call in isolation.

|*Test Steps:* | 1) Call `APP_DS18B20_GetLatest(NULL)`. +
2) Confirm no crash/fault occurs. +
3) Call `APP_DS18B20_GetLatest(&out)` with a valid pointer immediately after and confirm `out` still reflects the last legitimately stored value (unaffected by the NULL call).

|*Expected Results:*| No crash. `s_latest_reading` is unchanged by the NULL call.
|*Actual Results:*|
|===

---

==== UT-DS18B20-TASK-002 — GetLatest before any successful read reports invalid, 0.0°C

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-TASK-002

|Requirement
|SWRS-M1-SENS-01 — _pending confirmation, see Open Items OI-001_

|Objective
|Verify that before `APP_DS18B20_Task` has completed its first successful `DRV_DS18B20_Read()`, a caller of `APP_DS18B20_GetLatest()` receives the zero-initialised default (`valid == false`, `temperature_c == 0.0f`) rather than uninitialised memory.

|Preconditions
|Fresh boot / module reset. `APP_DS18B20_Task` has not yet been scheduled, or the 1-Wire bus is disconnected so the first `DRV_DS18B20_Read()` has not yet succeeded.

|*Test Steps:* | 1) On a fresh reset, before starting the scheduler (or with the sensor disconnected), call `APP_DS18B20_GetLatest(&out)`.

|*Expected Results:*| `out.valid == false`; `out.temperature_c == 0.0f` (static zero-initialisation of `s_latest_reading`, not garbage).
|*Actual Results:*|
|===

---

==== UT-DS18B20-TASK-003 — GetLatest reflects the most recent successful reading

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-TASK-003

|Requirement
|SWRS-SYS-03 (live sensor display), SWRS-M1-SENS-01*

|Objective
|Verify that once `APP_DS18B20_Task` is running against a live or HIL-emulated sensor, `APP_DS18B20_GetLatest()` returns a value that tracks the sensor within one polling cycle (≈750 ms at 12-bit resolution).

|Preconditions
|On-target. `APP_InitDrivers()` has run (`DRV_DS18B20_Init()` succeeded). `APP_DS18B20_Task` is scheduled and running. Sensor or HIL emulator (`sim_ds18b20`) present on the 1-Wire bus.

|*Test Steps:* | 1) On the HIL emulator, call `SIM_DS18B20_SetTemperature(300)` (30.0°C). +
2) Wait ≥ 1 s (one full poll cycle plus margin) for `APP_DS18B20_Task` to complete a read. +
3) Call `APP_DS18B20_GetLatest(&out)` from a separate task/context. +
4) Change the HIL set-point to `SIM_DS18B20_SetTemperature(50)` (5.0°C) and wait ≥ 1 s again. +
5) Call `APP_DS18B20_GetLatest(&out)` again.

|*Expected Results:*| Step 3: `out.valid == true`, `out.temperature_c` within ±0.0625°C of 30.0°C. Step 5: `out.valid == true`, `out.temperature_c` within ±0.0625°C of 5.0°C, confirming the published value tracks the sensor rather than being latched from the first read.
|*Actual Results:*|
|===

---

==== UT-DS18B20-TASK-004 — Task ignores DRV_DS18B20_Read() failures (defect / coding-standard gap)

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-TASK-004

|Requirement
|CIOET-ORGPORT-CGD-2026-001 Rule 9 (every non-void return value must be checked; on failure call `LIB_FEM_Report()`)

|Objective
|Document and verify the current (non-conformant) behaviour: `APP_DS18B20_Task` casts the return of `DRV_DS18B20_Read()` to `(void)` and never calls `LIB_FEM_Report()` on failure, so a disconnected sensor or persistent CRC fault is silent to the rest of the firmware — `s_latest_reading` simply stops updating and `out.valid` from the last successful read is never re-checked by callers unless they inspect the `valid` flag themselves.

|Preconditions
|On-target or HIL. `APP_DS18B20_Task` running with at least one prior successful read.

|*Test Steps:* | 1) Force a persistent bus fault (`SIM_DS18B20_SetFault(true)` on the HIL emulator, or physically disconnect the sensor). +
2) Wait several polling cycles. +
3) Call `APP_DS18B20_GetLatest(&out)`. +
4) Check whether any `EVT_WARNING`/`EVT_FAULT` event was reported via FEM/EEPROM for this condition.

|*Expected Results (current behaviour — FAIL against the coding standard):*| `out` retains the last successful reading indefinitely with `out.valid` from that last successful read (i.e. `true`), giving no indication that the sensor has since failed. No FEM event is logged for the fault. This is a *known gap*, not a pass — see Open Items OI-006 for the required fix (check the return value; on `false`, either set `s_latest_reading.valid = false` or call `LIB_FEM_Report()`, matching the pattern already used elsewhere per OP-SDP-001 §Chapter 15 Rule 9).
|*Actual Results:*|
|===

---

=== GPIO Open-Drain Direction Switching

`middleware/gpio/mw_gpio.c` (Shanid KT, commit `4d65d8f`) fixes `MW_GPIO_Init()` so that, when called with `MW_GPIO_DIR_INPUT` (the direction the 1-Wire driver switches to via `gpio_set_dir` — wired as `Gpio_PIF_SetDir`, which wraps `MW_GPIO_Init` — between every bit written and read), the output buffer (`OBE`) is explicitly disabled before the input buffer is enabled. Without this line, the pin would remain driven and the bus could never be pulled by the external resistor or by the DS18B20 — no 1-Wire transaction (reset, bit read, or bit write) would work at all. This same code path is shared with `drv_dht22`.

==== UT-DS18B20-GPIO-001 — MW_GPIO_Init(INPUT) disables the output buffer

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-GPIO-001

|Requirement
|OP-SDP-001 §4.4 (Driver Interface Pattern) — 1-Wire bus enabler for `drv_ds18b20`/`drv_dht22`

|Objective
|Verify that `MW_GPIO_Init()` called with `config->direction == MW_GPIO_DIR_INPUT` calls `Siul2_Port_Ip_SetOutputBuffer(port_base, local_pin, FALSE, PORT_MUX_AS_GPIO)` before enabling the input buffer, so the pin actually floats.

|Preconditions
|On-target, or host build with RTD `Siul2_Port_Ip_*` functions stubbed/mocked to record call arguments. Pin under test is the DS18B20 DQ pin (`APP_GPIO_DS18B20_DQ_PORT/PIN`).

|*Test Steps:* | 1) Call `MW_GPIO_Init()` with `direction = MW_GPIO_DIR_OUTPUT` on the DQ pin, then externally pull the pin LOW via a bench supply/resistor to ground and confirm (oscilloscope) the pin stays HIGH (driven, not floating) — negative control. +
2) Call `MW_GPIO_Init()` again with `direction = MW_GPIO_DIR_INPUT`. +
3) With the same external pull to ground applied, confirm (oscilloscope) the pin now reads LOW (floating, following the external pull) rather than being held HIGH by the MCU.

|*Expected Results:*| Step 1: pin remains HIGH regardless of external pull (output buffer enabled, driving). Step 3: pin follows the external pull to LOW (output buffer disabled, input buffer enabled). This is the regression this fix specifically guards against — before the fix, step 3 would also show the pin held HIGH.
|*Actual Results:*|
|===

---

==== UT-DS18B20-GPIO-002 — Repeated direction toggling survives many 1-Wire transactions

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-DS18B20-GPIO-002

|Requirement
|OP-SDP-001 §4.4

|Objective
|Verify that the OBE-clear fix is applied on *every* call to `MW_GPIO_Init(INPUT)`, not just the first — the code comment explicitly notes "OBE must be cleared each time" because open-drain buses re-enter output-driven state after every low pulse write. A single conversion cycle issues dozens of reset/read/write bit operations, each toggling direction.

|Preconditions
|On-target, DS18B20 or HIL emulator connected.

|*Test Steps:* | 1) Call `DRV_DS18B20_Read()` a single time (internally performs 2× `LIB_OneWire_Reset()` and one 72-bit `LIB_OneWire_ReadBlock()`, each bit toggling direction via `gpio_set_dir`). +
2) Confirm the read succeeds (`out.valid == true`). +
3) Repeat `DRV_DS18B20_Read()` 100 times back-to-back. +
4) Confirm all 100 reads succeed with no CRC failures or no-presence failures attributable to a stuck-output pin.

|*Expected Results:*| All 100 reads return `true` with `out.valid == true`. No intermittent CRC or presence failures caused by the pin failing to float on a later bit within the transaction.
|*Actual Results:*|
|===

---

=== Build and Scheduler Integration

Covers the non-functional changes required for `ds18b20_task` to actually run: FreeRTOS task table registration (`app/os/os_app_config.c`), heap sizing (`generate/include/FreeRTOSConfig.h`), and IDE build-path registration (`.cproject`, `.settings/language.settings.xml`) — all committed together in `0410e7c`.

==== TC-DS18B20-BUILD-001 — APP_DS18B20_Task is created and running

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|TC-DS18B20-BUILD-001

|Requirement
|OP-SDP-001 §9 (Release Plan) — task must be live for v0.0.2-alpha "first real sensor readings on UART"

|Objective
|Verify that the task table entry added to `APP_OS_task_config[]` (`task_function = APP_DS18B20_Task`, `task_name = "DS18B20"`, `stack_words = 256U`, `priority = 1U`) results in a running FreeRTOS task with the expected name, priority, and healthy stack margin.

|Preconditions
|Firmware built and flashed with the `ORPT-118-Temperature-sensor-DS18B20-implementation` branch. FreeRTOS trace/debugger available (e.g. Percepio Tracealyzer, or `vTaskList()` over debug UART).

|*Test Steps:* | 1) Boot the DUT. +
2) Query the FreeRTOS task list (`vTaskList()` or debugger RTOS-aware view). +
3) Locate the task named `"DS18B20"`. +
4) Record its state (should cycle Running/Blocked, never permanently Blocked or absent), priority, and high-water-mark stack usage (`uxTaskGetStackHighWaterMark()`).

|*Expected Results:*| Task `"DS18B20"` is present, priority `1`, and cycles between Running and Blocked (blocked during the 750 ms conversion delay inside `DRV_DS18B20_Read()`). Stack high-water-mark leaves at least 25% of the allocated 256 words unused (no risk of overflow).
|*Actual Results:*|
|===

---

==== TC-DS18B20-BUILD-002 — Heap increase accommodates the new task without exhaustion

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|TC-DS18B20-BUILD-002

|Requirement
|OP-SDP-001 §15 Rule 6 (no dynamic allocation after init — all FreeRTOS objects allocated at startup)

|Objective
|Verify that raising `configTOTAL_HEAP_SIZE` from 8192 to 16384 bytes is sufficient for all tasks created at startup (including `DS18B20`, `Stepper`, and `Wdg`, all added/enabled in the same commit) with headroom, and that no `configASSERT()` / heap-exhaustion fault fires during `APP_OS_InitTasks()`.

|Preconditions
|Full firmware image, all tasks in `APP_OS_task_config[]` enabled.

|*Test Steps:* | 1) Boot the DUT. +
2) After all startup task creation completes, call `xPortGetFreeHeapSize()` and `xPortGetMinimumEverFreeHeapSize()`. +
3) Confirm no `configASSERT()` fired during task/queue/mutex creation.

|*Expected Results:*| `xPortGetFreeHeapSize()` reports remaining headroom (target: ≥ 2 KB free after all startup allocation). No assertion failures. All tasks listed in TC-DS18B20-BUILD-001-style checks are present.
|*Actual Results:*|
|===

---

=== Hardware Verification (On-Target, DUT + Sensor / HIL)

==== TC-DS18B20-HW-001 — Temperature accuracy vs. calibrated reference

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|TC-DS18B20-HW-001

|Requirement
|OP-SDP-001 T-J2-07 acceptance criterion

|Objective
|Verify that a genuine DS18B20 wired to the S32K312 EVB reports temperature within ±0.5°C of a calibrated reference thermometer, at 12-bit resolution, across the expected clinical operating range.

|Preconditions
|`DRV_DS18B20_Init()` returns `true` on the DUT. Reference thermometer and DS18B20 co-located (same thermal mass), stabilised at each test point for ≥ 5 minutes.

|*Test Steps:* | 1) Place the DUT and reference thermometer in a temperature-controlled bath/chamber at 4°C (transport cold-chain low end). +
2) Wait for thermal stabilisation (≥ 5 min). +
3) Call `DRV_DS18B20_Read()` ten times, 1 s apart; record each `out.temperature_c`. +
4) Record the reference thermometer reading at the same time. +
5) Repeat steps 1–4 at 25°C (ambient) and at 37°C (body temperature, upper clinical bound). +
6) Compute the mean DS18B20 reading and the deviation from the reference at each test point.

|*Expected Results:*| At each of the three test points, the mean of the 10 DS18B20 readings is within ±0.5°C of the calibrated reference reading. `out.valid == true` for all 30 reads (no CRC failures).
|*Actual Results:*|
|===

---

==== TC-DS18B20-HW-002 — Conversion time matches configured resolution

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|TC-DS18B20-HW-002

|Requirement
|OP-SDP-001 §5.3 (drv_time_if_t / DWT microsecond timing)

|Objective
|Verify that the blocking duration of `DRV_DS18B20_Read()` matches the conversion time for the configured resolution (750 ms at 12-bit) within tolerance, confirming `cfg.delay_ms` is being driven with the correct value from `DS18B20_CONV_MS[]`.

|Preconditions
|`DRV_DS18B20_Init()` returns `true` on the DUT. `cfg.resolution = DRV_DS18B20_RES_12BIT`. A GPIO pin is toggled immediately before and after the call to `DRV_DS18B20_Read()` for oscilloscope capture.

|*Test Steps:* | 1) Toggle a spare GPIO HIGH immediately before calling `DRV_DS18B20_Read()`. +
2) Call `DRV_DS18B20_Read()`. +
3) Toggle the GPIO LOW immediately after the call returns. +
4) Capture the HIGH pulse width on an oscilloscope. +
5) Repeat for `DRV_DS18B20_RES_9BIT` (expected ≈94 ms), `DRV_DS18B20_RES_10BIT` (≈188 ms), and `DRV_DS18B20_RES_11BIT` (≈375 ms).

|*Expected Results:*| Measured pulse width for each resolution is within +20 ms / −0 ms of the corresponding `DS18B20_CONV_MS[]` value (94, 188, 375, 750 ms), accounting for 1-Wire reset/command overhead on top of the conversion wait.
|*Actual Results:*|
|===

---

==== TC-DS18B20-HW-003 — HIL fault injection: no-presence detected in Read

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|TC-DS18B20-HW-003

|Requirement
|SWRS-M1-SENS-01 — _pending confirmation, see Open Items OI-001_

|Objective
|Verify on real 1-Wire electrical timing (not the host stub) that `DRV_DS18B20_Read()` returns `false` when the HIL emulator withdraws its presence pulse mid-session, simulating a broken sensor connection.

|Preconditions
|DUT running application firmware with `drv_ds18b20` wired to the HIL emulator (`sim_ds18b20`) on the configured 1-Wire GPIO pin. `SIM_DS18B20_Init()` has been called on the Arduino and a successful `DRV_DS18B20_Read()` has completed at least once.

|*Test Steps:* | 1) On the Arduino, call `SIM_DS18B20_SetFault(true)` to withdraw the presence pulse. +
2) On the DUT, call `DRV_DS18B20_Read(&cfg, &out)`. +
3) Observe the return value and `out.valid`. +
4) On the Arduino, call `SIM_DS18B20_SetFault(false)` to restore presence. +
5) Call `DRV_DS18B20_Read(&cfg, &out)` again and observe recovery.

|*Expected Results:*| Step 3: `DRV_DS18B20_Read()` returns `false`, `out.valid == false`. Step 5: `DRV_DS18B20_Read()` returns `true` and `out.valid == true`, confirming the driver recovers cleanly once the sensor is reconnected (no latched fault state).
|*Actual Results:*|
|===

---

==== TC-DS18B20-HW-004 — HIL: set-point temperature round-trip accuracy

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|TC-DS18B20-HW-004

|Requirement
|SWRS-M1-SENS-01 — _pending confirmation, see Open Items OI-001_

|Objective
|Verify that a known scratchpad temperature value injected via the HIL emulator is decoded correctly end-to-end through real 1-Wire bus timing (reset, command bytes, 9-byte scratchpad read, CRC-8 check, 0.0625°C/LSB scaling).

|Preconditions
|DUT running application firmware with `drv_ds18b20` wired to the HIL emulator. `SIM_DS18B20_Init()` has been called.

|*Test Steps:* | 1) On the Arduino, call `SIM_DS18B20_SetTemperature(250)` (25.0°C). +
2) On the DUT, call `DRV_DS18B20_Read(&cfg, &out)`. +
3) Repeat with `SIM_DS18B20_SetTemperature(-100)` (−10.0°C) and `SIM_DS18B20_SetTemperature(850)` (85.0°C, DS18B20 power-on-reset default).

|*Expected Results:*| For each injected value, `DRV_DS18B20_Read()` returns `true`, `out.valid == true`, and `out.temperature_c` matches the injected value within ±0.0625°C (one LSB at 12-bit resolution).
|*Actual Results:*|
|===

---

== Test Traceability Matrix

[cols="1,2,1,1,1", options="header"]
|===
|Test ID |Title |Requirement |Risk Class (IEC 62304) |Execution

|UT-DS18B20-INIT-001 |Init — NULL config pointer |SWRS-M1-SENS-01* |C |Automated
|UT-DS18B20-INIT-002 |Init — NULL delay_ms |SWRS-M1-SENS-01* |C |Automated
|UT-DS18B20-INIT-003 |Init — NULL ow.gpio_set |SWRS-M1-SENS-01* |C |Automated
|UT-DS18B20-INIT-004 |Init — NULL ow.gpio_read |SWRS-M1-SENS-01* |C |Automated
|UT-DS18B20-INIT-005 |Init — NULL ow.gpio_set_dir |SWRS-M1-SENS-01* |C |Automated
|UT-DS18B20-INIT-006 |Init — NULL ow.delay_us |SWRS-M1-SENS-01* |C |Automated
|UT-DS18B20-INIT-007 |Init — invalid resolution |SWRS-M1-SENS-01* |C |Automated
|UT-DS18B20-INIT-008 |Init — no device present |SWRS-M1-SENS-01* |C |Automated
|UT-DS18B20-INIT-009 |Init — device present, valid CRC |SWRS-M1-SENS-01* |C |Automated
|UT-DS18B20-READ-001 |Read — NULL config pointer |SWRS-M1-SENS-01* |C |Automated
|UT-DS18B20-READ-002 |Read — NULL output pointer |SWRS-M1-SENS-01* |C |Automated
|UT-DS18B20-READ-003 |Read — no device on reset |SWRS-M1-SENS-01* |C |Automated
|UT-DS18B20-READ-004 |Read — all-zero scratchpad, valid CRC |SWRS-M1-SENS-01* |C |Automated
|UT-DS18B20-TASK-001 |GetLatest — NULL output pointer is a no-op |SWRS-M1-SENS-01* |C |Automated
|UT-DS18B20-TASK-002 |GetLatest before first successful read |SWRS-M1-SENS-01* |C |Automated
|UT-DS18B20-TASK-003 |GetLatest tracks live/HIL sensor value |SWRS-SYS-03 |C |Manual
|UT-DS18B20-TASK-004 |Task ignores Read() failures (defect) |CGD-2026-001 Rule 9 |C |Manual
|UT-DS18B20-GPIO-001 |MW_GPIO_Init(INPUT) disables output buffer |OP-SDP-001 §4.4 |C |Manual
|UT-DS18B20-GPIO-002 |Direction toggling survives 100 reads |OP-SDP-001 §4.4 |C |Manual
|TC-DS18B20-BUILD-001 |APP_DS18B20_Task created and running |OP-SDP-001 §9 |C |Manual
|TC-DS18B20-BUILD-002 |Heap increase sufficient, no assert |OP-SDP-001 §15 Rule 6 |C |Manual
|TC-DS18B20-HW-001 |Temperature accuracy vs. calibrated reference |OP-SDP-001 T-J2-07 |C |Manual
|TC-DS18B20-HW-002 |Conversion time matches resolution |OP-SDP-001 §5.3 |C |Manual
|TC-DS18B20-HW-003 |HIL fault injection — no-presence in Read |SWRS-M1-SENS-01* |C |Manual
|TC-DS18B20-HW-004 |HIL set-point temperature round-trip |SWRS-M1-SENS-01* |C |Manual

|===

_*Requirement ID SWRS-M1-SENS-01 is an assumed allocation pending confirmation — see Open Items OI-001._

[[manual-hardware-test-cases-summary]]
== Manual / Hardware Test Cases Summary

The following 10 test cases are marked `Manual` in the Test Traceability Matrix above. They require physical hardware (calibrated reference thermometer, oscilloscope, RTD register access, or the Arduino HIL emulator) and are not exercised by the host-build `test_runner`. Work through these individually using the detailed Test Steps in each test case's own section.

[cols="1,2,3", options="header"]
|===
|Test ID |Title |Why Manual

|UT-DS18B20-TASK-003 |GetLatest tracks live/HIL sensor value |Requires a live sensor or the Arduino `sim_ds18b20` emulator plus a running scheduler; not exercised by the host stub build.

|UT-DS18B20-TASK-004 |Task ignores Read() failures (defect) |Requires forcing a persistent bus fault (HIL or physical disconnect) and inspecting FEM/EEPROM logs on-target.

|UT-DS18B20-GPIO-001 |MW_GPIO_Init(INPUT) disables output buffer |Requires an oscilloscope/voltmeter on the DQ pin with an external pull applied, and real RTD `Siul2_Port_Ip_*` register access.

|UT-DS18B20-GPIO-002 |Direction toggling survives 100 reads |Requires a live sensor or HIL emulator to exercise real 1-Wire bus timing 100 times consecutively.

|TC-DS18B20-BUILD-001 |APP_DS18B20_Task created and running |Requires a FreeRTOS-aware debugger view or `vTaskList()` over debug UART on the DUT.

|TC-DS18B20-BUILD-002 |Heap increase sufficient, no assert |Requires on-target heap introspection (`xPortGetFreeHeapSize()`) after full startup.

|TC-DS18B20-HW-001 |Temperature accuracy vs. calibrated reference |Requires a calibrated reference thermometer and a temperature-controlled bath/chamber at three set points.

|TC-DS18B20-HW-002 |Conversion time matches configured resolution |Requires an oscilloscope on a GPIO toggled around the blocking `DRV_DS18B20_Read()` call.

|TC-DS18B20-HW-003 |HIL fault injection — no-presence in Read |Requires the Arduino `sim_ds18b20` emulator wired to the DUT's 1-Wire pin.

|TC-DS18B20-HW-004 |HIL set-point temperature round-trip |Requires the Arduino `sim_ds18b20` emulator wired to the DUT's 1-Wire pin.

|===

NOTE: `UT-DS18B20-TASK-001` and `UT-DS18B20-TASK-002` are listed as `Automated` in the Test Traceability Matrix because they require no hardware, but as of this revision **no test file exists yet for `ds18b20_task.c`** (there is no `test_ds18b20_task.c` alongside `test_drv_ds18b20.c` in `common-libs/tests/`, and `app/ds18b20/` is not part of the host-build `Makefile`). They cannot be checked off against a passing console log until that harness is written — see Open Items OI-007.

---

== Open Items

[cols="1,1,3,1", options="header"]
|===
|ID |Priority |Description |Owner

|OI-001
|High
|Requirement ID for the DS18B20 channel (`SWRS-M1-SENS-01` used as a placeholder throughout this document) must be confirmed against `CIOET-ORGPORT-SWRS-S32K312-2026-001` (the node-specific SWRS, not yet reviewed here) before this spec can be formally reviewed.
|Systems Engineering

|OI-002
|Medium
|1-wire GPIO pin assignment for the DS18B20 (Arduino header, per OP-SDP-001 OI-04) must be finalised and documented in the Test Environment section before TC-DS18B20-HW-001/002/003/004 can be executed on hardware.
|Firmware Team (Junior 2)

|OI-003
|Medium
|Confirm whether a second DS18B20 channel (`temp_2` in `sensor_frame_t` / TPDO2) uses an independent instance of this same driver on a separate GPIO, requiring a duplicated hardware test pass, or shares the 1-Wire bus via ROM search (out of scope for this driver revision).
|Firmware Team

|OI-004
|Low
|Tolerance band for TC-DS18B20-HW-002 (+20 ms / −0 ms) is an initial estimate for 1-Wire reset/command overhead on top of the datasheet conversion time; confirm against a measured baseline once hardware is available.
|Firmware Team

|OI-005
|Medium
|`app/gpio/gpio_app_config.c` comments the DS18B20 DQ pin as "PTC20" while `app/gpio/GPIO_app_config.h` defines `APP_GPIO_DS18B20_DQ_PORT/PIN` as `MW_GPIO_PORT_A`, pin `2` (i.e. PTA2) — the comment and the macro disagree. Confirm the actual wired pin against the schematic (OP-SDP-001 OI-04) and correct whichever of the two is wrong before UT-DS18B20-GPIO-001/002 and the TC-DS18B20-HW-* tests are executed.
|Firmware Team (Junior 2 / Shanid)

|OI-006
|High
|`APP_DS18B20_Task` (`app/ds18b20/ds18b20_task.c`) discards the return value of `DRV_DS18B20_Read()` with a `(void)` cast and never calls `LIB_FEM_Report()` on failure, in violation of `CIOET-ORGPORT-CGD-2026-001` Rule 9. A disconnected sensor or persistent CRC fault currently produces no EEPROM event and leaves `APP_DS18B20_GetLatest()` silently returning a stale reading. Fix required before this can be marked PASS in UT-DS18B20-TASK-004 — track under an ORPT ticket.
|Shanid KT

|OI-007
|High
|No host-build unit test file exists for `ds18b20_task.c` (`ds18b20_task.c`/`.h` are also not yet added to `common-libs/tests/Makefile` — they live in `orgport-app-s32k312/app/`, not `common-libs/`, so they need either a small app-layer host harness or an on-target test hook). `UT-DS18B20-TASK-001`/`002` cannot be executed until this is created.
|Shanid KT

|OI-008
|Medium
|`configTOTAL_HEAP_SIZE` was raised from 8192 to 16384 in the same commit that enabled the `Stepper` and `Wdg` tasks alongside `DS18B20` — TC-DS18B20-BUILD-002 as written cannot isolate how much of the new headroom is actually attributable to the DS18B20 task alone. Acceptable for a system-level check; note if a component-level heap budget is later required per task.
|Firmware Team

|===

---

_End of document UT-SPEC-DRV-DS18B20-001 v0.1_
