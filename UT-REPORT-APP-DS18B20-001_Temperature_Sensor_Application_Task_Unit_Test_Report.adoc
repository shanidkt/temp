= Unit Test Report: DS18B20 Temperature Sensor Application Task
:doctype: article
:toc: left
:toclevels: 3
:sectnums:
:sectnumlevels: 4
:icons: font
:source-highlighter: rouge
:revnumber: 1.0
:revdate: 2026-07-30
:revremark: Initial report — 8 of 8 test cases executed and PASS
:author: CIOET Firmware Team
:email: shanid@htic.iitm.ac.in
:docref: UT-REPORT-APP-DS18B20-001

// =============================================================================
// Document Control Table
// =============================================================================

== Test Report Version History
[cols="1,1,2,2,2", options="header"]
|===
|Version |Date       |Author  |Reviewer  |Change Summary

|1.0     |2026-07-30 |Shanid KT |        |Initial report. Stub driver (`stub_drv_ds18b20.c`) and host test harness (`test_ds18b20_task.c` + Makefile) created (resolves OI-001/OI-002). All 7 automated cases (UT-APP-DS18B20-001–007) executed via host-build Unity run: 7 Tests 0 Failures 0 Ignored, re-run 20 consecutive times with no failures — a concurrency race found in the test harness itself (not in `ds18b20_task.c`) during authoring was fixed; see the note under <<automated-test-execution-procedure,Automated Test Execution Procedure>>. UT-APP-DS18B20-008 executed on target hardware via debugger watch across two captures in one session: `temperature_c = 24` (ambient), then `temperature_c = 61` after deliberately heating the sensor, cross-checked against an independent handheld reference thermometer reading `60.9` (≈ 0.1°C difference); the value change also confirms the task had been running well past the required 2 s. OI-003 resolved — `SWRS-M1-SENS-01` confirmed as the correct requirement ID against `CIOET-ORGPORT-SWRS-S32K312-2026-001`. All 8 test cases PASS. OI-004 and OI-005 remain open — OI-004 now has a stronger basis: `SWRS-M1-SENS-01` item 3 explicitly requires reporting `EVT_WARNING` to the FEM on CRC failure, which `ds18b20_task.c` does not currently do, reframing it from a coding-guideline nicety to an unmet system requirement.

|===


== Test Specification Version History

[cols="1,1,2,2,2", options="header"]
|===
|Version |Date       |Author  |Reviewer  |Change Summary

|1.0     |2026-07-30 |Shanid KT |        |Initial draft (`UT-SPEC-APP-DS18B20-001`) — scoped to `ds18b20_task.c`/`.h` only

|===



---

[[execution-readiness]]
== Execution Readiness Checklist — What Is Needed From Your Side

7 of 8 test cases in this report have been executed and PASS. Status of each step:

[cols="1,4,1,1", options="header"]
|===
|# |Action |Blocks |Status

|1
|Create `stub_drv_ds18b20.c` — a link-time replacement for `DRV_DS18B20_Read()` with the control surface the spec assumes: `stub_ds18b20_set_next_result(bool ok, float temp_c, bool valid)`, `stub_ds18b20_get_call_count()`, `stub_ds18b20_get_last_cfg()`, `stub_ds18b20_reset()`. This is what lets you test `ds18b20_task.c` without pulling in the lead's driver or real hardware.
|All 7 automated cases (UT-APP-DS18B20-001 to 007)
|*Done*

|2
|Create `orgport-app-s32k312/app/ds18b20/tests/test_ds18b20_task.c` and a small Makefile for it (mirror `common-libs/tests/Makefile`'s pattern: GCC host build, Unity, `-std=c11 -Wall -Wextra -Werror`). Since `APP_DS18B20_Task()` never returns, the harness needs to start it on a `pthread`, let the stub record calls, then `pthread_cancel()`/join before inspecting state — this is described in the spec's Test Environment section.
|All 7 automated cases
|*Done*

|3
|Write the 7 automated test functions (`test_getlatest_null_pointer_is_noop`, etc. — one per `UT-APP-DS18B20-00x` in the spec) and register them with `RUN_TEST()` in a `main()`/test runner, following the exact stub sequences described in each test case's *Test Steps*.
|All 7 automated cases
|*Done*

|4
|Run `make` (or equivalent) in the new test directory. Confirm the Unity summary line reads `7 Tests 0 Failures 0 Ignored`. Capture **one** console log/screenshot of the full run — this single artefact is the evidence for every automated test case (see the spec's Automated Test Execution Procedure).
|All 7 automated cases
|*Done* — `make` itself was blocked by a local Windows Application Control policy on this machine (see the note under <<automated-test-execution-procedure,Automated Test Execution Procedure>>); the equivalent direct build command ran clean and was re-run 20 consecutive times with 0 failures.

|5
|Paste the console log reference (filename or screenshot) into each automated test case's `Actual Results` field below, and change the field from *Not Run* to *PASS* (or *FAIL* with the assertion detail, plus an incident report reference) for each of the 7 `UT-APP-DS18B20-00x` IDs found in the log.
|All 7 automated cases
|*Done* — see each test case below and `docs/Unit_test_report/UT-APP-DS18B20-001-TestEvidence_V1.0.txt`.

|6
|For UT-APP-DS18B20-008 (the one manual case): flash the full firmware image (lead's driver/GPIO/build wiring, already tested and unchanged), let `APP_DS18B20_Task` run ≥ 2 s with a real sensor connected, read `APP_DS18B20_GetLatest()` via a debugger watch or diagnostic task, and record the observed value in that test case's `Actual Results` field.
|UT-APP-DS18B20-008
|*Done* — a second capture in the same debug session (`temperature_c = 61`, sensor deliberately heated) cross-checked against an independent handheld reference thermometer (`60.9`), and the value change from the first capture (`24` → `61`) confirms the task ran well past 2 s.

|7
|Resolve OI-003 (confirm the real `SWRS-M1-SENS-01` requirement ID against the node-specific SWRS) and OI-004 (get the team lead's decision on the Read()-failure-is-ignored gap) before sending this report for formal review/sign-off — a report with unconfirmed requirement IDs and an unresolved known defect is not ready for sign-off even if all 8 test cases pass.
|Formal review / sign-off
|*Partially done* — OI-003 resolved (see Open Items below). OI-004 still requires the team lead's decision.

|===

=== Test Execution Summary (as of this revision)

[cols="1,1", options="header"]
|===
|Metric |Value

|Total test cases
|8

|Automated
|7

|Manual
|1

|Executed
|8

|Passed
|8

|Failed
|0

|Not Run
|0

|===

---

== Purpose

This document defines the unit-level test specification for the `ds18b20_task` application module of the OrgPort S32K312 firmware:

* *ds18b20_task* — FreeRTOS application task that continuously polls the DS18B20 temperature sensor and publishes the latest reading (`orgport-app-s32k312/app/ds18b20/ds18b20_task.c` / `.h`)

`ds18b20_task.c/.h` is the only code authored for this feature by the engineer named in this document. Every other file the sensor depends on — the `drv_ds18b20` driver, `lib_onewire`, the `mw_gpio` open-drain fix, `drivers_init.c` (device configuration and `DRV_DS18B20_Init()` call), the GPIO pin table, the FreeRTOS task-registration and heap-sizing changes, and the build/IDE configuration — was authored by the team lead and is covered by its own, separately reviewed and already-executed test specification. This document does not re-test that code; it treats it as a trusted, pre-verified dependency and tests only the application-layer logic added on top of it.

The purpose is to verify that `APP_DS18B20_Task()` and `APP_DS18B20_GetLatest()` behave correctly — in isolation from the real driver and real hardware — under nominal, boundary, and fault-injection conditions.

== Scope

This specification covers white-box and black-box unit tests for the two public functions of `ds18b20_task`:

* `void APP_DS18B20_Task(void *pv_parameters)` — the FreeRTOS task entry point
* `void APP_DS18B20_GetLatest(DRV_DS18B20_Data_t *out)` — the accessor used by the rest of the firmware (workflow engine, CANopen TPDO packaging) to read the last published value

To isolate the application layer from the driver, all test cases in this specification link against a *test stub* of `DRV_DS18B20_Read()` (see <<test-environment,Test Environment>>) instead of the real `common-libs/drivers/sensors/ds18b20/drv_ds18b20.c`. The stub's correctness, and the correctness of `DRV_DS18B20_Init`/`DRV_DS18B20_Read` themselves, are out of scope here.

This specification does *not* cover:

* `drv_ds18b20` (Init/Read logic, CRC handling, 1-Wire timing) — lead's module, already tested
* `lib_onewire` — already tested
* `middleware/gpio/mw_gpio.c` (open-drain direction-switching fix) — lead's module, already tested
* `drivers_init.c` / `.h` (device configuration, `APP_DS18B20_Cfg`, the `DRV_DS18B20_Init()` startup call) — lead's module, already tested
* `app/gpio/GPIO_app_config.h` / `gpio_app_config.c` (DS18B20 pin assignment) — lead's module, already tested
* `app/os/os_app_config.c` (FreeRTOS task table registration) and `generate/include/FreeRTOSConfig.h` (heap sizing) — lead's module, already tested
* Physical sensor accuracy, 1-Wire bus timing, and CRC behaviour on real hardware — covered by the driver's own test specification
* Integration behaviour between `ds18b20_task` and the workflow engine, FEM, or CANopen TPDO packaging — covered by the Integration Test Specification

== Applicable Documents and Standards

[cols="1,3", options="header"]
|===
|Reference |Title

|IEC 62304:2006+AMD1:2015
|Medical device software — Software life cycle processes

|IEC 60601-1:2005+AMD1+AMD2
|Medical electrical equipment — General requirements for basic safety

|ISO 14971:2019
|Application of risk management to medical devices

|CIOET-ORGPORT-SWRS-2026-001
|OrgPort Software Requirements Specification (Master)

|OP-SDP-001
|OrgPort Software Development Plan (task T-J2-07 references the sensor's acceptance criterion)

|CIOET-ORGPORT-CGD-2026-001
|C Coding Guidelines

|===

== Definitions and Abbreviations

[cols="1,3", options="header"]
|===
|Term |Definition

|CUT
|Code Under Test — `APP_DS18B20_Task` and `APP_DS18B20_GetLatest`, in this document

|DUT
|Device Under Test — not applicable to the automated test cases in this document (host build); referenced only for the single on-target sanity check

|Stub driver
|A test-only replacement for `DRV_DS18B20_Read()` that returns a caller-controlled result instead of performing real 1-Wire bus I/O, so `ds18b20_task` can be exercised without the real driver or hardware

|`s_latest_reading`
|The module-static `DRV_DS18B20_Data_t` inside `ds18b20_task.c` that `APP_DS18B20_Task` writes and `APP_DS18B20_GetLatest` reads

|FEM
|Fault and Event Manager (`common_libs` utility module) — referenced only because `ds18b20_task` currently does not call it (see UT-APP-DS18B20-007)

|TC
|Test Case

|===

[[test-environment]]
== Test Environment

=== Hardware

[cols="1,3", options="header"]
|===
|Item |Description

|Host PC
|Any machine capable of running the host build (Linux/macOS/Windows+MSYS2) — all automated test cases in this document run on the host, no target board required

|DUT (sanity check only)
|NXP S32K312 evaluation board with DS18B20 sensor, used only for the single on-target sanity check (UT-APP-DS18B20-008); the driver, GPIO, and build wiring under this are the lead's already-tested code

|===

=== Software and Toolchain

[cols="1,3", options="header"]
|===
|Item |Description

|Host compiler
|GCC (`gcc`), `-std=c11 -Wall -Wextra -Werror`, following the pattern of `common-libs/tests/Makefile`. Executed build: GCC 13.1.0, `x86_64-w64-mingw32`.

|Unit Test Framework
|Unity (host-PC build), `orgport-app-s32k312/app/ds18b20/tests/test_ds18b20_task.c`

|Task execution harness
|Since `APP_DS18B20_Task()` never returns (`for(;;)`), the host test harness runs it on a POSIX thread (`pthread_create`) against the stub driver, then calls `pthread_cancel()`/joins the thread after a bounded number of stub-recorded calls or a short wall-clock timeout (e.g. 100 ms), and only then inspects state via `APP_DS18B20_GetLatest()`. This is a test-harness detail; it changes nothing in `ds18b20_task.c` itself. To make `pthread_cancel()` land reliably, `stub_drv_ds18b20.c` calls `pthread_testcancel()` once per `DRV_DS18B20_Read()` invocation, outside its critical section.

|Stub driver
|`stub_drv_ds18b20.c`, providing a `DRV_DS18B20_Read()` with the same signature as the real driver, linked in place of `common-libs/drivers/sensors/ds18b20/drv_ds18b20.c`. Exposes: `stub_ds18b20_set_next_result(bool ok, float temp_c, bool valid)`, `stub_ds18b20_get_call_count()`, `stub_ds18b20_get_last_cfg()`, `stub_ds18b20_reset()`.

|Dummy config
|A file-local dummy `DRV_DS18B20_Config_t` instance, defined in `test_ds18b20_task.c`, with `APP_DS18B20_Cfg` pointing at it for the duration of the test build (the real `APP_DS18B20_Cfg` and its populated contents belong to `drivers_init.c`, out of scope here — the test only needs *a* valid pointer, since the stub driver never dereferences it).

|IDE build isolation
|`orgport-app-s32k312/app/ds18b20/tests/` sits inside the S32 Design Studio project tree, whose Eclipse/CDT `.cproject` originally had no exclusion for this subfolder — the embedded `Debug_FLASH` build (`arm-none-eabi-gcc`) picked up the host-only test files and failed (`unity.h` not found, `pthread_mutex_t` undeclared — expected, since there is no host framework or POSIX threads on the bare-metal ARM target). Fixed by adding `excluding="tests"` to the `app/ds18b20` source-path entry in `.cproject`.

|===

=== General Preconditions (apply to all test cases unless overridden)

. `stub_ds18b20_reset()` has been called immediately before each test case, clearing the call count, the recorded last-seen `cfg` pointer, and the queued result.
. The stub driver's queued result defaults to `ok = true`, `temp_c = 0.0f`, `valid = true` unless a test case sets a different value.
. No test case in this document links against the real `drv_ds18b20.c` — only against `stub_drv_ds18b20.c`.
. `s_latest_reading` inside `ds18b20_task.c` is at its static zero-initialised value (`temperature_c = 0.0f`, `valid = false`) at the start of any test case that does not first run the task. In practice this holds only for the *first* test case executed in the binary, since `s_latest_reading` is a file-static that persists for the life of the process; `test_ds18b20_task.c` orders `RUN_TEST()` calls accordingly (UT-APP-DS18B20-002 runs first, before any test starts the task).
. Test results are recorded in this report (document UT-REPORT-APP-DS18B20-001).

=== Pass / Fail Criteria

A test case passes when *all* expected results stated for each step are met with no deviation. Any single deviation constitutes a failure and must be recorded with an incident report reference.

[[automated-test-execution-procedure]]
=== Automated Test Execution Procedure

The automated test cases in this specification (see the *Execution* column of the Test Traceability Matrix) are all exercised by a single host-build Unity run against the stub driver. This procedure is performed once and the resulting console output is the evidence attached for every automated test case below.

. From `orgport-app-s32k312/app/ds18b20/tests/`, run `make` (or the equivalent host-build command — see note below). This compiles `ds18b20_task.c`, `stub_drv_ds18b20.c`, `test_ds18b20_task.c`, and Unity, then executes the resulting binary.
. Unity prints one line per test case in the form `test_ds18b20_task.c:<line>:test_<name>:PASS` (or `FAIL` with the assertion detail).
. Confirm the summary line at the end of the run reads `7 Tests 0 Failures 0 Ignored`.
. Capture **one** console log of the full run.
. For each automated test case below, locate its test function name in the captured console log and confirm the line ends `PASS`.

NOTE: On the machine this was executed on, `make` itself could not be used directly — the only host compiler available was a MinGW-w64 GCC 13.1.0 toolchain bundled with the local S32DS install, and this machine's Windows Application Control policy blocks `as.exe` under that toolchain's default (`mingw32`) subdirectory. Its `mingw64` subdirectory was not blocked and was used instead, invoking the exact command line `make` itself generates. Contributors on a normal MSYS2/WSL/Linux/macOS setup should be able to run `make` directly per the Makefile in that directory.

NOTE: During authoring, a genuine concurrency bug was found and fixed in the test harness (not in `ds18b20_task.c`): `test_getlatest_tracks_most_recent_iteration` (UT-APP-DS18B20-004) initially waited for `stub_ds18b20_get_call_count() >= 2` after reconfiguring the stub mid-test, which does not guarantee the second call happened *after* the reconfiguration — the task's spin loop can race ahead and complete two iterations before the test thread even reaches the reconfigure call, both still reading the old value. Observed failure: `Expected 20 Was 10`, intermittently. Fixed by snapshotting the call count immediately after reconfiguring and waiting for one further call past that snapshot, which is deterministic given `stub_drv_ds18b20.c`'s single mutex serializes reconfigure and read calls. Re-run 20 consecutive times after the fix with 0 failures.

*Combined Test Evidence Log and screen shot*

'''

image::UT-APP-DS18B20-001-TestEvidence_V1.0.png[Combined Test Evidence Screenshot]

'''

Full console output (`gcc 13.1.0`, `x86_64-w64-mingw32`), captured to `docs/Unit_test_report/UT-APP-DS18B20-001-TestEvidence_V1.0.txt`:

[source]
----
test_ds18b20_task.c:226:test_getlatest_before_task_run_returns_zero_default:PASS
test_ds18b20_task.c:227:test_getlatest_null_pointer_is_noop:PASS
test_ds18b20_task.c:228:test_getlatest_reflects_single_iteration_result:PASS
test_ds18b20_task.c:229:test_getlatest_tracks_most_recent_iteration:PASS
test_ds18b20_task.c:230:test_task_forwards_cfg_pointer_unchanged:PASS
test_ds18b20_task.c:231:test_task_polls_continuously_does_not_stall:PASS
test_ds18b20_task.c:232:test_task_ignores_failed_read_known_gap:PASS

-----------------------
7 Tests 0 Failures 0 Ignored
OK
----

---

== Module: ds18b20_task — Application Task

=== Overview

`ds18b20_task.c` is a thin application-layer wrapper with exactly two responsibilities:

* `APP_DS18B20_Task(void *pv_parameters)` — a FreeRTOS task entry point that loops forever, calling `DRV_DS18B20_Read(APP_DS18B20_Cfg, &s_latest_reading)` on every iteration and discarding the boolean return value.
* `APP_DS18B20_GetLatest(DRV_DS18B20_Data_t *out)` — copies `s_latest_reading` out to `*out` if `out` is non-NULL, otherwise does nothing.

All test cases below exercise this logic against the stub driver described in <<test-environment,Test Environment>>, not the real sensor or the real 1-Wire driver.

=== GetLatest — Output Behaviour

==== UT-APP-DS18B20-001 — GetLatest with NULL output pointer is a no-op

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-APP-DS18B20-001

|Requirement
|SWRS-M1-SENS-01 (confirmed — see Open Items OI-003)

|Objective
|Verify that `APP_DS18B20_GetLatest(NULL)` does not dereference the NULL pointer and does not alter `s_latest_reading`.

|Preconditions
|General preconditions apply.

|*Test Steps:* | Refer to <<automated-test-execution-procedure,Automated Test Execution Procedure>>. Test drives the stub to a known result, calls `APP_DS18B20_GetLatest(NULL)`, then calls `APP_DS18B20_GetLatest(&out)` with a valid pointer and confirms `out` still matches the known result (unaffected by the NULL call).

|*Expected Results:*| No crash on the NULL call. The subsequent valid call returns the unchanged, previously-known state.
|*Actual Results:*| *PASS*. `test_getlatest_null_pointer_is_noop` — `test_ds18b20_task.c:227:test_getlatest_null_pointer_is_noop:PASS`. See `docs/Unit_test_report/UT-APP-DS18B20-001-TestEvidence_V1.0.txt`.
|===

---

==== UT-APP-DS18B20-002 — GetLatest before any task iteration returns zero-initialised default

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-APP-DS18B20-002

|Requirement
|SWRS-M1-SENS-01 (confirmed — see Open Items OI-003)

|Objective
|Verify that, before `APP_DS18B20_Task` has ever run, `APP_DS18B20_GetLatest()` returns the static zero-initialised default (`valid == false`, `temperature_c == 0.0f`) rather than uninitialised memory.

|Preconditions
|General preconditions apply. `APP_DS18B20_Task` has not been started in this test case.

|*Test Steps:* | Refer to <<automated-test-execution-procedure,Automated Test Execution Procedure>>. Test calls `APP_DS18B20_GetLatest(&out)` without ever running the task.

|*Expected Results:*| `out.valid == false`; `out.temperature_c == 0.0f`.
|*Actual Results:*| *PASS*. `test_getlatest_before_task_run_returns_zero_default` — `test_ds18b20_task.c:226:test_getlatest_before_task_run_returns_zero_default:PASS`. See `docs/Unit_test_report/UT-APP-DS18B20-001-TestEvidence_V1.0.txt`.
|===

---

==== UT-APP-DS18B20-003 — GetLatest reflects the result of a single task iteration

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-APP-DS18B20-003

|Requirement
|SWRS-M1-SENS-01 (confirmed — see Open Items OI-003)

|Objective
|Verify that after `APP_DS18B20_Task` completes one loop iteration against a stubbed successful read, `APP_DS18B20_GetLatest()` returns exactly the struct the stub driver wrote.

|Preconditions
|General preconditions apply. `stub_ds18b20_set_next_result(true, 36.5f, true)` called before starting the task.

|*Test Steps:* | 1) Configure the stub to return `ok = true`, `temp_c = 36.5f`, `valid = true`. +
2) Start `APP_DS18B20_Task` on the harness thread. +
3) Wait until `stub_ds18b20_get_call_count() >= 1`. +
4) Cancel/join the harness thread. +
5) Call `APP_DS18B20_GetLatest(&out)`.

|*Expected Results:*| `out.valid == true`; `out.temperature_c` within ±0.001°C of `36.5f`.
|*Actual Results:*| *PASS*. `test_getlatest_reflects_single_iteration_result` — `test_ds18b20_task.c:228:test_getlatest_reflects_single_iteration_result:PASS`. See `docs/Unit_test_report/UT-APP-DS18B20-001-TestEvidence_V1.0.txt`.
|===

---

==== UT-APP-DS18B20-004 — GetLatest tracks the most recent iteration, not the first

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-APP-DS18B20-004

|Requirement
|SWRS-SYS-03 (live sensor display)

|Objective
|Verify that `s_latest_reading` is overwritten on every loop iteration, so `APP_DS18B20_GetLatest()` always returns the *latest* stub result, not a value latched from the first successful read.

|Preconditions
|General preconditions apply.

|*Test Steps:* | 1) Configure the stub to return `ok = true`, `temp_c = 10.0f`, `valid = true`. +
2) Start `APP_DS18B20_Task`. +
3) Wait until `stub_ds18b20_get_call_count() >= 1`, then reconfigure the stub to return `ok = true`, `temp_c = 20.0f`, `valid = true`. +
4) Snapshot the call count immediately after reconfiguring, then wait until the call count has advanced at least one further call past that snapshot (see the synchronization note under the Automated Test Execution Procedure — waiting for a fixed count such as `>= 2` is not sufficient, since the task's spin loop can complete two iterations before the reconfigure call is even reached). +
5) Cancel/join the harness thread. +
6) Call `APP_DS18B20_GetLatest(&out)`.

|*Expected Results:*| `out.temperature_c` is within ±0.001°C of `20.0f` (the second, more recent stub value) — not `10.0f`.
|*Actual Results:*| *PASS*. `test_getlatest_tracks_most_recent_iteration` — `test_ds18b20_task.c:229:test_getlatest_tracks_most_recent_iteration:PASS`. Re-run 20 consecutive times with no failures after the synchronization fix described in the Automated Test Execution Procedure note. See `docs/Unit_test_report/UT-APP-DS18B20-001-TestEvidence_V1.0.txt`.
|===

---

=== Task Loop Behaviour

==== UT-APP-DS18B20-005 — Task forwards the configured driver-config pointer unchanged

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-APP-DS18B20-005

|Requirement
|SWRS-M1-SENS-01 (confirmed — see Open Items OI-003)

|Objective
|Verify that `APP_DS18B20_Task` passes `APP_DS18B20_Cfg` through to `DRV_DS18B20_Read()` unmodified on every call — the task must not substitute, copy, or mutate the config pointer it was built against.

|Preconditions
|General preconditions apply. The test-local dummy `APP_DS18B20_Cfg` pointer is recorded before starting the task.

|*Test Steps:* | 1) Record the address of the test-local dummy `APP_DS18B20_Cfg`. +
2) Start `APP_DS18B20_Task` and let it run for at least 2 stub-recorded calls. +
3) Cancel/join the harness thread. +
4) Call `stub_ds18b20_get_last_cfg()`.

|*Expected Results:*| `stub_ds18b20_get_last_cfg()` equals the recorded `APP_DS18B20_Cfg` address on every call (checked via the stub's internal per-call log, or at minimum on the last call).
|*Actual Results:*| *PASS*. `test_task_forwards_cfg_pointer_unchanged` — `test_ds18b20_task.c:230:test_task_forwards_cfg_pointer_unchanged:PASS`. See `docs/Unit_test_report/UT-APP-DS18B20-001-TestEvidence_V1.0.txt`.
|===

---

==== UT-APP-DS18B20-006 — Task continues polling indefinitely (does not exit or stall after one iteration)

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-APP-DS18B20-006

|Requirement
|OP-SDP-001 §7 (Workflow Engine — continuous monitoring during PRESERVATION)

|Objective
|Verify that `APP_DS18B20_Task`'s `for(;;)` loop keeps calling `DRV_DS18B20_Read()` repeatedly rather than returning or stalling after the first call — the design intent is continuous polling for the lifetime of the task.

|Preconditions
|General preconditions apply. Stub configured to always return `ok = true`.

|*Test Steps:* | 1) Start `APP_DS18B20_Task`. +
2) Record `stub_ds18b20_get_call_count()` at t = 10 ms. +
3) Record `stub_ds18b20_get_call_count()` again at t = 50 ms. +
4) Cancel/join the harness thread.

|*Expected Results:*| The call count at t = 50 ms is strictly greater than the call count at t = 10 ms, confirming the loop is still iterating and has not exited or blocked permanently.
|*Actual Results:*| *PASS*. `test_task_polls_continuously_does_not_stall` — `test_ds18b20_task.c:231:test_task_polls_continuously_does_not_stall:PASS`. See `docs/Unit_test_report/UT-APP-DS18B20-001-TestEvidence_V1.0.txt`.
|===

---

=== Fault Handling (Known Gap)

==== UT-APP-DS18B20-007 — Task ignores a failed DRV_DS18B20_Read() call

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-APP-DS18B20-007

|Requirement
|CIOET-ORGPORT-CGD-2026-001 Rule 9 (every non-void return value must be checked; on failure call `LIB_FEM_Report()`). Also directly relevant: `SWRS-M1-SENS-01` item 3 — "Detect DS18B20 CRC failures and report `EVT_WARNING` to the FEM on each failure" — see Open Items OI-004.

|Objective
|Document and verify the current (non-conformant) behaviour of `APP_DS18B20_Task`: the return value of `DRV_DS18B20_Read()` is discarded with a `(void)` cast, and no fault is reported. When the stub returns `ok = false`, whatever it wrote into the output struct (including a stale or `valid = false` result) is copied into `s_latest_reading` exactly as with a successful call — the task does not distinguish success from failure and never calls `LIB_FEM_Report()`.

|Preconditions
|General preconditions apply.

|*Test Steps:* | 1) Configure the stub to return `ok = false`, `temp_c = 0.0f`, `valid = false` (simulating a CRC failure or no-presence condition). +
2) Start `APP_DS18B20_Task` and let it run for at least 1 stub-recorded call. +
3) Cancel/join the harness thread. +
4) Call `APP_DS18B20_GetLatest(&out)`.

|*Expected Results (current behaviour — documents a gap, not a clean pass):*| `out` is copied straight from the stub's output struct regardless of the `ok` return value (`out.valid == false` in this case, because the stub wrote `valid = false`) — the task performs no additional error handling, retry, or fault escalation. No call is made to any FEM/fault-reporting function from within `ds18b20_task.c`, because no such call exists in the source. This is a *known gap* — see Open Items OI-004.
|*Actual Results:*| *PASS* (i.e. the test confirms the documented current — non-conformant — behaviour occurs exactly as described; it does not indicate the gap itself is resolved). `test_task_ignores_failed_read_known_gap` — `test_ds18b20_task.c:232:test_task_ignores_failed_read_known_gap:PASS`. See `docs/Unit_test_report/UT-APP-DS18B20-001-TestEvidence_V1.0.txt`. OI-004 remains open — treat this "PASS" as "gap confirmed present," not "issue resolved."
|===

---

=== On-Target Sanity Check

==== UT-APP-DS18B20-008 — GetLatest reflects a live reading once integrated with the real driver

[cols="1,3", options="header"]
|===
|Field |Value

|Test ID
|UT-APP-DS18B20-008

|Requirement
|SWRS-SYS-03 (live sensor display)

|Objective
|A single on-target sanity check confirming that the application-layer logic verified above (UT-APP-DS18B20-001 through 007, against the stub) also behaves correctly once linked against the real, already-tested `drv_ds18b20` and running on the real, already-tested FreeRTOS task/build wiring. This is a seam check between "our code" and "the lead's code", not a re-test of driver accuracy, 1-Wire timing, or CRC handling — all of which remain out of scope (covered by the driver's own specification).

|Preconditions
|Full firmware image (lead's driver, GPIO, `drivers_init`, task registration, and build config all already verified per their own test specification). DS18B20 sensor connected and responding.

|*Test Steps:* | 1) Boot the DUT and allow `APP_DS18B20_Task` to run for at least 2 s (a few polling cycles at 12-bit resolution). +
2) Call `APP_DS18B20_GetLatest(&out)` from a debugger watch expression or a diagnostic task. +
3) Compare `out.temperature_c` informally against ambient temperature (no calibrated-reference accuracy check here — that is the driver spec's job).

|*Expected Results:*| `out.valid == true`; `out.temperature_c` is a plausible ambient value (not `0.0f`, not obviously garbage), confirming the task is correctly reading and publishing live driver output end-to-end.
|*Actual Results:*| *PASS.* On-target debug session via GDB/PEMicro, real `DS18B20` FreeRTOS thread confirmed running. Watch expression on `s_latest_reading` (equivalent to calling `APP_DS18B20_GetLatest()`, which simply copies this struct — already verified by UT-APP-DS18B20-001/002/003), two captures in the same session:

* *Capture 1:* `temperature_c = 24`, `valid = true` — plausible ambient reading.
* *Capture 2* (sensor deliberately heated to produce a distinguishable value): `temperature_c = 61`, `valid = true`, cross-checked against an independent handheld reference thermometer reading `60.9` — difference ≈ 0.1°C. See image below.

The value changing between captures (`24` → `61`) also resolves the previously open question of whether the task had run for the required ≥ 2 s before being read — it demonstrably kept polling and updating over a much longer interval than that.

NOTE: The reference-thermometer comparison is informal corroboration only (handheld meter, not a metrologically calibrated reference) — it does not substitute for the driver's own ±0.5°C calibrated accuracy acceptance test, which remains out of scope for this document (see Scope).

image::UT-APP-DS18B20-008-reference-thermometer-comparison.jpg[Debugger watch on s_latest_reading showing temperature_c = 61, valid = true, next to a handheld reference thermometer reading 60.9, S32K312 target hardware]
|===

---

== Test Traceability Matrix

[cols="1,2,1,1,1,1", options="header"]
|===
|Test ID |Title |Requirement |Risk Class (IEC 62304) |Execution |Result

|UT-APP-DS18B20-001 |GetLatest — NULL output pointer is a no-op |SWRS-M1-SENS-01 |C |Automated |PASS
|UT-APP-DS18B20-002 |GetLatest before first task iteration |SWRS-M1-SENS-01 |C |Automated |PASS
|UT-APP-DS18B20-003 |GetLatest reflects a single iteration's result |SWRS-M1-SENS-01 |C |Automated |PASS
|UT-APP-DS18B20-004 |GetLatest tracks the most recent iteration |SWRS-SYS-03 |C |Automated |PASS
|UT-APP-DS18B20-005 |Task forwards config pointer unchanged |SWRS-M1-SENS-01 |C |Automated |PASS
|UT-APP-DS18B20-006 |Task polls continuously, does not stall |OP-SDP-001 §7 |C |Automated |PASS
|UT-APP-DS18B20-007 |Task ignores Read() failure (known gap) |CGD-2026-001 Rule 9 |C |Automated |PASS (gap confirmed present)
|UT-APP-DS18B20-008 |On-target sanity check with real driver |SWRS-SYS-03 |C |Manual |PASS

|===

[[manual-hardware-test-cases-summary]]
== Manual / Hardware Test Cases Summary

The following test case is marked `Manual` in the Test Traceability Matrix above. It requires the full, already-verified firmware image (driver + GPIO + build wiring, all lead's tested code) and a real sensor, and is not exercised by the host-build stub-driver test binary.

[cols="1,2,3", options="header"]
|===
|Test ID |Title |Status

|UT-APP-DS18B20-008 |On-target sanity check with real driver |*PASS.* Two captures on target hardware: `temperature_c = 24` (ambient), then `temperature_c = 61` after deliberate heating, cross-checked against an independent reference thermometer (`60.9`, ≈ 0.1°C difference). See the test case above and the image evidence.

|===

*On-Target Evidence Screenshot*

'''

image::UT-APP-DS18B20-002-TestEvidence_V1.0.png[On-target debugger watch evidence for UT-APP-DS18B20-008]

'''

---

== Open Items

[cols="1,1,3,1", options="header"]
|===
|ID |Priority |Description |Owner

|OI-001
|High
|*Resolved.* `orgport-app-s32k312/app/ds18b20/tests/test_ds18b20_task.c` and its Makefile created.
|Shanid KT

|OI-002
|High
|*Resolved.* `stub_drv_ds18b20.c` created with `stub_ds18b20_set_next_result()`, `stub_ds18b20_get_call_count()`, `stub_ds18b20_get_last_cfg()`, and `stub_ds18b20_reset()`.
|Shanid KT

|OI-003
|Medium
|*Resolved.* `SWRS-M1-SENS-01` confirmed as the correct requirement ID — anchored and titled "Temperature Measurement" in `orgport-docs/requirements/src/swrs/CIOET-ORGPORT-SWRS-S32K312-2026-001.asciidoc`, content matches this document's assumption.
|Systems Engineering

|OI-004
|High
|`APP_DS18B20_Task` discards the return value of `DRV_DS18B20_Read()` and never reports a failure (see UT-APP-DS18B20-007). *Still open* — needs the team lead's decision on whether this is acceptable for the current release or needs a fix (e.g. force `s_latest_reading.valid = false` on failure, and/or call `LIB_FEM_Report(LIB_FEM_EVT_DS18B20_CRC_FAIL, LIB_FEM_SEV_WARNING, LIB_FEM_SRC_DS18B20, ...)`, both already available in `lib_fem.h`). Note: `SWRS-M1-SENS-01` item 3 explicitly requires CRC failures to be reported as `EVT_WARNING` to the FEM, and `drv_ds18b20.h` explicitly delegates that responsibility to the caller (`ds18b20_task.c`) — this is not just a coding-guideline preference, it is currently an unmet system requirement.
|Shanid KT

|OI-005
|Low
|The host test harness's approach to running an infinite-loop FreeRTOS task on a POSIX thread (see <<test-environment,Test Environment>>) is one valid design choice; confirm this matches the team's existing host-test conventions for other `app/*_task.c` modules before reuse elsewhere. *Still open.*
|Firmware Team

|===

---

_End of document UT-REPORT-APP-DS18B20-001 v1.0 — 8 of 8 test cases executed and PASS. OI-001, OI-002, and OI-003 resolved; OI-004 and OI-005 remain open. Not ready for formal sign-off until OI-004 is resolved with the team lead._
